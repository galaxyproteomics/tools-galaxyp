<tool id="i2nca_agnostic_qc_2" name="Create an Agnostic QC Report with i2nca v2" version="@TOOL_VERSION@+galaxy@GALAXY_VERSION@">

	<description>
        Tes for decrep.
        Create agnostic Quality control reports for any kind of .imzML file.
        Different metrics will be visualized as a pdf report. 
    </description>
	
    <macros>
    <import>macros.xml</import>
    </macros>
    
    <requirements>
    <!-- We'll skip the reqirements for now. Later we get a biocontainer, for now we'll do wonky conda env stuff-->
    </requirements>

    <!-- ? 2DO w/ Niko: better do deal with CLI or cheetath -->
    <command detect_errors="exit_code"><![CDATA[
    cp "${infile.extra_files_path}/imzml" indata.imzML &&
    cp "${infile.extra_files_path}/ibd" indata.ibd &&
    'i2nca_agnostic_qc' 'indata.imzML' './data'
    ]]></command>

    <!-- ? ? 2DO w/ Niko: Adiitional documents that are optional -->
    <inputs>
        <param name="infile" type="data" format="imzml" label="MSI data" help="oad the .imzml file on which the QC should be performed"/>
    </inputs>

    <!-- ? ? 2DO w/ Niko: where to store pdf files -->
    <outputs>
            <data name="AgnosticQC" label="Agnostic QC report on ${on_string}" from_work_dir="data_agnostic_QC.pdf" format="pdf" />
    </outputs>

    <help><![CDATA[
        ow wth collection?
    ]]></help>
</tool>

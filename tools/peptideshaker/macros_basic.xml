<macros>
    <xml name="stdio">
        <stdio>
            <exit_code range="1:" level="fatal" description="Job Failed" />
            <regex match="OutOfMemoryError" level="fatal_oom" description="Out of memory"/>
            <regex match="java.*Exception" level="fatal" description="Java Exception"/>
            <regex match="Could not reserve enough space for * object heap" level="fatal_oom" description="Out of memory"/>
            <regex match="Could not create the Java virtual machine" level="fatal" description="JVM Error"/>
            <regex match="PeptideShaker ran out of memory" level="fatal_oom" description="JVM Error"/>
            <regex match="CompomicsError" level="fatal" description="Compomics Error"/>
        </stdio>
    </xml>
    <token name="@PROFILE@">24.2</token>
    <token name="@SEARCHGUI_MAJOR_VERSION@">0</token>
    <token name="@SEARCHGUI_VERSION@">4.3.15</token>
    <token name="@SEARCHGUI_VERSION_SUFFIX@">2</token>
    <token name="@PEPTIDESHAKER_VERSION@">3.0.11</token>
    <token name="@PEPTIDESHAKER_VERSION_SUFFIX@">0</token>
    <xml name="citations">
        <citations>
            <citation type="doi">10.1186/1471-2105-12-70</citation>
            <citation type="doi">10.1002/pmic.201000595</citation>
            <citation type="doi">doi:10.1038/nbt.3109</citation>
        </citations>
    </xml>

    <token name="@LINK_PEAK_LISTS@">
        #for $peak_list_file in $peak_lists_files:
            #set $input_name = ""
            #if $peak_list_file.is_of_type("mgf"):
                #set $input_name = $peak_list_file.element_identifier.split('/')[-1].replace(".mgf", "") + ".mgf"
            #else if $peak_list_file.is_of_type("mzml"):
                #set $input_name = $peak_list_file.element_identifier.split('/')[-1].replace(".mzml", "") + ".mzml"
            #end if
            cp '${peak_list_file}' '${input_name}';
        #end for
    </token>

    <xml name="peak_lists">
        <param name="peak_lists_files" format="mgf,mzml" type="data" multiple="true" label="Input Peak Lists" help="Select appropriate MGF/MZML dataset(s) from history. Remember to choose only .mzML files when using MetaMorpheus." />
    </xml>


</macros>

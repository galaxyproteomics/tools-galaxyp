<tool id="msspsmtable_merge" name="Merge collection of tabular files with headers" version="1.0">
    <description>from many to one PSM table</description>
    <requirements>
        <requirement type="package" version="2.2">msstitch</requirement>
    </requirements>
    <command>
	    msspsmtable merge -o '$output' -i
            #for $key in $mzidtsv.keys()
            '$mzidtsv[$key]'
            #end for
    </command>
    
    <inputs>
        <param name="mzidtsv" type="data_collection" format="mzidtsv" 
		label="Search table from MSGF+"/>
    </inputs>
    
    <outputs>
        <data format="mzidtsv" name="output"/>
    </outputs>

    <help>
	    Takes a multifile TSV table with MSGF+ data and collapses it into one file.
	    Headers of the individual files are checked to make sure they are identical,
	    which should be implicit in multifile datasets.
    </help>

</tool>

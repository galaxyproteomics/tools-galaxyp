<tool id="msslookup_protspace" name="Create lookup DB for whole protein filtering" version="1.0">
    <description>from FASTA</description>
    <requirements>
        <requirement type="package" version="2.2">msstitch</requirement>
    </requirements>
    <command>
            <![CDATA[
	    msslookup protspace -i '$input' --minlen $minlength 
	    &&
	    mv mslookup_db.sqlite '$output'
            ]]>
    </command>
    
    <inputs>
        <param name="input" type="data" format="fasta" 
		label="FASTA database" help="Contains the protein sequences 
		that will be used to create the lookup table" />
        <param name="minlength" type="integer" label="Minimum length of peptides that will be filtered" value="" />
    </inputs>
    <outputs>
        <data format="mslookupsql" name="output"/>
    </outputs>

    <help>
	    Creates DB lookup from a FASTA sequence file. Lookup will contain
            all possible peptide sequences of certain length from all proteins in the DB
    </help>
</tool>

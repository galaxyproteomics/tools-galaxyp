diff --git a/tools/msstitch/linted/calculate_allset_fdr.xml b/tools/msstitch/linted/calculate_allset_fdr.xml
index 984abb3..be17ec6 100644
--- a/tools/msstitch/linted/calculate_allset_fdr.xml
+++ b/tools/msstitch/linted/calculate_allset_fdr.xml
@@ -10,6 +10,19 @@
   
     
     <command interpreter="python">
+	    <!--
+		 ### CAN WE LEAVE THIS TO A WF? PROBABLY, test with tessttolshed!
+		 # step 0, not in wf, check if more sets, then dont run wf
+		 # merge target/decoy peptide tables
+		 # sed Peptide sequence?, then psm2pep wo quant
+		 # protein fdr on peptide tables? why?
+		 # 
+		 # cut -f protein table accession col to avoid polluting final table
+		 # if picked, etc: modelqvals on pep table
+		 # bestpeptide, pickedfdr on intermediate table
+		 # join (if dicts are used for output, otherwise paste) tables
+
+		 -->
 	    <![CDATA[
 	    #if len($teps) == 1
 	       echo Only one sample set in peptides found, copying input protein table to output file

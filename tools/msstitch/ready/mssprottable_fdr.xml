<tool id="prottable_fdr" name="Add protein FDR"> 
    <requirements>
        <requirement type="package" version="2.2">msstitch</requirement>
    </requirements>
  
    <description>from qvality to protein table</description>
    <command>
	    mssprottable fdr -i '$input' -o '$output'
	    --qvality '$qvalityfile' --scorecolpattern $scorecol
    </command>
    
    <inputs>
      <param name="input" type="data" format="prottable" label="Protein table" />
      <param name="qvalityfile" type="data" format="qvality" label="Qvality data" />
      <param name="scorecol" type="text" label="Regular expression pattern to determine score column" help="E.g. ^Q-score" />
    </inputs>
    
    <outputs>
        <data format="prottable" name="output"/>
    </outputs>

    <help>
	    This tool reads output from percolator's qvality tool and then adds FDR values
	    to the protein table, by matching protein scores to the qvality table and reading
	    the respective q-value and PEP.
    </help>

</tool>

<tool id="msspsmtable_specdata" name="Add experimental and spectra data">
    <requirements>
        <requirement type="package" version="2.2">msstitch</requirement>
    </requirements>
  
    <description>from sqlite database to PSM table</description>
    
    <command>
	    cp '$lookup' spec_lookup.sql
	    &&
	    msspsmtable specdata -i '$input' -o '$output' --dbfile spec_lookup.sql
    </command>
    
    <inputs>
      <param name="input" type="data" format="mzidtsv" label="TSV of MSGF+ output" />
      <param name="lookup" type="data" format="mslookupsql" label="Lookup file for fetching quant and spectra info from" />
    </inputs>
    
    <outputs>
        <data format="mzidtsv" name="output"/>
    </outputs>

    <help>
	    Add some data from a lookup to the PSM table. Columns added are in this version
	    biological set, scan number, and retention time.
    </help>

</tool>

<tool id="msspercolator_filteruni" name="Filter unique peptides">
    <requirements>
        <requirement type="package" version="2.2">msstitch</requirement>
    </requirements>
  
    <description>from percolator data</description>
    
    <command>
	    msspercolator filteruni -i '$input' -o '$output' --score $score
	    --score $score
    </command>
    
    <inputs>
        <param name="input" type="data" format="percout" label="Percolator output"/>
    <param name="score" type="select" label="Score to use as filter">
        <option value="svm">svm score</option>
        <option value="pep">PEP</option>
        <option value="q">q-value</option>
        <option value="p">p-value</option>
    </param>
    </inputs>
    
    <outputs>
        <data format="percout" name="output"/>
    </outputs>

    <help>
	    Takes a Percolator output file that has duplicate peptides in it, due to
	    e.g. a merge operation. Peptides are uniqued by keeping the best
	    scoring ones, while all PSMs are retained.
    </help>

</tool>
